# Logs (geral, para todas as pastas)
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*
lerna-debug.log*

# Arquivos de configuração de editores e IDEs
.vscode/*
!.vscode/extensions.json  # Mantém apenas o arquivo de extensões recomendadas
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Compilados e builds (geral)
dist/
dist-ssr/
build/
out/
bin/
obj/

# Dependências (geral)
node_modules/

# Arquivos locais e temporários
*.local
.env
.env.*
!.env.example  # Mantém um arquivo de exemplo, se você tiver um
*.tmp
*.temp

# Específico do Frontend (React com Vite)
frontend/node_modules/
frontend/dist/
frontend/.cache/ # Cache do Vite
frontend/.parcel-cache/ # Cache do Parcel, se você usar em algum lugar
# Gerenciados pelo package manager
frontend/package-lock.json
frontend/yarn.lock
frontend/pnpm-lock.yaml

# Específico do Backend (Spring Boot)
backend/target/
backend/build/
backend/.mvn/
backend/mvnw
backend/mvnw.cmd
.classpath
.project
.settings/
!backend/src/main/resources/application.properties #Mantem o application.properties

# Específico do Database (PostgreSQL)
database/data/          # Dados locais do PostgreSQL (não devem ser commitados)
database/pg_log/        # Logs do PostgreSQL
database/backup/        # Backups locais do banco de dados
*.sql.gz               # Backups compactados

# Outros
coverage/             # Relatórios de cobertura de testes
.nyc_output/          # Saída do NYC (cobertura de testes)
*.egg-info/           # Metadados de pacotes Python (se aplicável)
.pytest_cache/        # Cache do pytest (se aplicável)
__pycache__/          # Cache do Python (se aplicável)
*.pyc                  # Arquivos compilados do Python (se aplicável)