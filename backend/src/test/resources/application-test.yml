spring:
  application:
    name: backend-dev
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;INIT=CREATE SCHEMA IF NOT EXISTS test_schema\\;SET SCHEMA test_schema\\;;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update # ou create-drop se precisar recriar sempre
    show-sql: false # true para debug, mas false para testes mais limpos
    properties:
      hibernate:
        format_sql: true
jwt:
  secret: ZXKFvAUFF6NwvQpw0AkzgsTqHT4iQSqb7MkEsTarhPU=
  expirationMs: 86400000
  refreshExpiration: 604800000
admin:
  email: admin@suportflow.com.br
  password: suportflow-admin
file:
  upload:
    dir: /tmp/uploads
