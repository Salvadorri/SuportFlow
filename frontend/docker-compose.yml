services:
  suporteflow:
    build:
      context: .
      dockerfile: Dockerfile-dev
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
    command: pnpm run dev

    networks:
      - suporteflow-network
networks:
  suporteflow-network:
    driver: bridge
